class AppConstants {
  // Flutter Web (Chrome) → localhost
  // Android emulator → 10.0.2.2
  // Physical device → your PC's local IP e.g. 192.168.1.5
  static const String serverBase = 'http://192.168.1.13:3000';
  static const String baseUrl = '$serverBase/api';
}
